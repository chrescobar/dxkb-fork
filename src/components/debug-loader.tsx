"use client";

import "../scripts/debug-bookmarklet";

// This component exists only to import a client-side debugging helper so
// developers can toggle outlines and run dxkbLayoutInfo() from the console.
export function DebugLoader(): null {
  return null;
}
